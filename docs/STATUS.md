# STATUS — where we are, what's next

**Read this first when resuming.** Single source for current progress. Last updated **12 Jul 2026**.

> 🏃 **Want to run/demo the prototype?** See **[prototype-demo-guide.md](app/prototype-demo-guide.md)** — how to run, a step-by-step manual-test walkthrough (what to do + what you should see), test accounts, and how to verify the backend.

## One-line state
**App well past the vertical slice; AI is live; SOCIAL + EXPERTS clusters built; PREMIUM UPGRADE FLOW live; PTD + PUM assembled.** Backend on Supabase: 26 tables + RLS + RPCs (`end_workout_session`, `add_friend`/`remove_friend`, `challenge_leaderboards`, service-request transitions + `submit_expert_review`, `start_premium`) + **two Edge Functions (`summarise-progress`, `suggest-plan`) running OpenAI gpt-4o-mini** (Gemini → rule fallback); a **local CLI stack** (ports 55321-9) now mirrors hosted. Flutter app (BCE, **221 tests**, Android + iOS verified): vertical slice ▸ 5-tab nav ▸ Profile cluster ▸ onboarding wizard ▸ AI plans both tiers ▸ My Plans + Plan Detail #8 ▸ connected devices #7.1 ▸ **Social cluster (#11 feed + likes/comments, #11.1 Post Detail, #11.2 User Profile + mutual friends, #11.3 Challenges w/ live leaderboards)** ▸ **Experts marketplace (#6/#6.1/#6.2 + MY PURCHASES + the complete #20–#24 expert portal incl. #21.2 service editor + #24.1 professional info; simulated payment)** ▸ **Premium upgrade (#16 Upgrade + #13.6 Subscription Management, simulated checkout, live role flip)** ▸ **Notifications US19–21 (rule-based local reminders + #13.4 UPCOMING strip)** ▸ Free month-cap ▸ MET calories ▸ custom workout types/health tags ▸ white-base semantic theme + shared widget library (`ui/common/`). Trackers: [requirements/user-stories.md](requirements/user-stories.md) (**51 ✅ · 10 🟨 · 3 ⬜**) · [testing/bug-log.md](app/testing/bug-log.md). **The app is FEATURE-COMPLETE (10 Jul)** — app tail, demo polish (seed athletes + [demo-script.md](app/demo-script.md)), the finishing pass (Training Effect, avatar upload, real BLE), and the [module test report](app/testing/module-test-report.md) are all done. Remaining (all outside `app/lib`): **1) one physical-device pass** (notification delivery + real-BLE pairing) · **2) demo rehearsals** from the script · ~~3) Admin WEB portal~~ ✅ **built 12 Jul** (Vue routes at `web/#/admin` — US52–US64 realised, see below) · **4) PRD/SRS/TDM reconciliation edits** ([doc-reconciliation-log](deliverables/doc-reconciliation-log.md)) · **5) website deploy** — the marketing site lives at `web/` and **shares the app's Supabase DB** (live reads + real Auth, 11 Jul); only hosting/deployment remains. (Details below.)

## 12 Jul — ADMIN WEB PORTAL (US41/52–62/64 ✅ · US63 🟨)
- **The portal lives inside the website** (`web/` Vue app, routes under `/admin`) — the natural home since the site already had Supabase Auth login, the shared DB, and `is_admin()` write policies on every managed table. `AdminLayout` guards every admin route (re-resolves the session's profile role on load; non-admins bounce to `/login`); the login page routes admin accounts straight to `/admin`; sign-out ends the session. Pages: **Overview** (live tier tiles + "needs attention" counts), **Users** (search, suspend/reactivate, tier switch free ↔ premium — expert only via approval, admin never grantable), **Expert applications** (approve → verified + expert role via the new `review_expert_application` RPC; reject → rejected), **Service listings** (all statuses incl. drafts; archive/restore — archiving hides from the app instantly), **Categories** (add/retire/reactivate), **Pricing** (price/description/visibility per plan), **Testimonials** (approve/reject with optional admin reply), **Feedback** (new/reviewed), **Contact inbox** (respond + resolve, #28.1).
- **Backend** (migration `20260712090000_admin_portal.sql`, local + hosted): the `review_expert_application(p_expert, p_approve)` SECURITY DEFINER RPC (admin-checked; approval also flips `profiles.role` — the role-guard trigger already lets admins through) and an `expert_services_admin_all` policy (writes were owner-only; reads live-only). Everything else rides existing `is_admin()` policies. New demo account **admin@wiseworkout.test / Password123!** (Ava Admin) in seed-demo §1 + created on hosted.
- **Verified end-to-end in a headless browser** against the local stack: guard bounce, admin login → overview, a seeded application (Noah Reyes) approved → user row shows EXPERT + expert_profiles verified in psql, suspend → SUSPENDED → reactivate, pending testimonial approved, contact resolved with recorded response, feedback reviewed, a live listing archived and restored. All test rows cleaned; migration + admin account applied to hosted (advisors: only the intentional authenticated-callable RPC warning, same pattern as every existing RPC).
- **Deliberate limits (documented):** password reset rides the shared pre-auth flow (no portal page) · landing feature copy/media stay static (US63 🟨) · responses to contact messages are recorded, not emailed · expert *content* (US30/48) remains out of scope per WBS.

## 11 Jul (later) — WEBSITE ON THE SHARED DATABASE (US05 ✅)
- **One DB for both products** (migration `20260711090000_landing_site.sql`, local + hosted): the drafts the site shipped with became real — `public_testimonials` (admin-moderated, anon reads approved), `landing_pricing_plans` ($9.99/mo, seeded via `seed.sql`), `landing_media_assets`, an anon read policy on `expert_categories`, and two anon-callable SECURITY DEFINER functions — `landing_metric_summary()` (live counts; growth %s stay client-side, no history to derive them) and `landing_featured_experts()` (verified + non-suspended only, no emails). `contact_messages` already existed with anon insert by design.
- **Signup trigger v3**: mirrors `last_name` too, and an `expert_application` object in the signup metadata creates the **pending** `expert_profiles` row + `expert_verification_documents` metadata inside the trigger (exception-guarded so bad metadata never blocks account creation) — so the US06 application works even when email confirmation delays the session. Role stays `free` until admin approval.
- **Web gateways rewired** (`supabaseClient.ts` + 5 gateways): metrics/pricing/testimonials/experts/categories read live with the bundled seed as offline fallback; register/login/expert-application use real Supabase Auth (site drops the session after signup — accounts are for the app); contact inserts a real row. `.env.local` targets the local stack; default = hosted (same publishable key as the app).
- **Verified end-to-end**: headless-browser runs against the local stack — landing showed live counts + Sam Rivera + the seeded Mia/Alex/Jordan/Priya testimonials; register → login ("Welcome back, Webby → /home"); expert application → pending profile + 2 doc rows; contact → `open` row (all checked in psql, then cleaned). Hosted: RPCs verified as anon via REST; signup-trigger test account created + verified + removed. ⚠️ Hosted quirks: email confirmation is ON (confirm before login) and undeliverable domains (`.test`) are rejected at signup. Advisors: only the two intentionally-anon landing functions flagged (by design; rest pre-existing).
- **Demo-seed additions**: §10 approved testimonials (Mia/Alex/Jordan/Priya) + the pricing catalog — applied to local and hosted.

## 11 Jul — WEBSITE IMPORTED INTO THE MONOREPO + DOCS SPLIT
- **`web/` is now the marketing website** (moved from the standalone `fyp-26-s2-37-landing-page` folder, which was never a git repo — nothing lost): Vue 3 + Vite + TypeScript with the same BCE discipline (`src/boundary|controller|entity`, `npm run check:bce` enforces it), landing sections + `/register` + `/expert-application` + `/login` UIs, seed-backed gateways, and `web/database/migrations/` draft add-ons for the shared Supabase DB (`public_testimonials`, `landing_pricing_plans`, `landing_media_assets`, `landing_metric_summary` — **not applied**). Run: `cd web && npm install && npm run dev`; verify: `npm run verify` (check-bce + type-checked build). **Still pending: live Supabase reads/writes + Auth + deploy** — tracked as US01–US06 🟨 in [requirements/user-stories.md](requirements/user-stories.md).
- **Website rethemed to the app's design system (11 Jul):** the dark lime/black landing theme was replaced with the app's white-base semantic palette (`docs/app/reference/palette.md` — soft-white bg, white cards + uniform soft shadow + 16px radii, electric-violet #7B2FF7 accent, semantic success/danger/premium, app metric colours on the statistics tiles) and the iOS-style system type stack (Barlow Condensed / DM Mono / DM Sans dropped, Google Fonts import removed). All CSS variables renamed `--lime*` → `--accent*`; verified with `npm run verify` + full-page screenshots.
- **`docs/` split by product:** mobile-app docs moved to **`docs/app/`** (architecture/, reference/, testing/, prototype-demo-guide, demo-script, simplify); the website's own docs landed in **`docs/web/`** (plan, limitations, test plan, demo checklist, presentation guide, database changes); project-wide files stay at the root (this file, README index, requirements/, deliverables/, scripts/, archive/). All cross-links repo-wide re-pointed and link-checked (only the known intentionally-unresolvable React-mock provenance links remain).

## 9 Jul — MANUAL WORKOUT ENTRY (US13 ✅)
- **"Log a workout manually"** on Train → `ManualEntryScreen`: type chips from the catalog, date/time pickers (future starts blocked), duration, distance (cardio only), feel pills, private notes. The `LogManualWorkout` control reuses the whole capture pipeline — insert with `connected_device_id = null` → the same `end_workout_session` RPC → summary write — so **XP/streak/level-up and MET calories are identical to tracked sessions** (verified live: +75 XP for a 30-min 5 km run; History row shows pace + an HR dash).
- **RPC change** (migration `20260709090000`, local + hosted): `end_workout_session` honours an optional backdated `started_at` in the metrics (ended_at = start + duration, capped at now; future starts raise). Live sessions unchanged.
- **190 tests** (+2: manual payload incl. backdate + no-device, non-cardio drops distance).

## 10 Jul (later) — MODULE TEST REPORT (for the 11 Jul milestone)
- **[testing/module-test-report.md](app/testing/module-test-report.md)** generated from a fresh run: 9 modules · **221/221 automated cases pass** (each enumerated by name from the live suite) · 13 manual procedures indexed to their dated STATUS evidence · requirements traceability per module · declared limitations (sim can't exercise notification delivery / real-BLE hardware; payment simulated by scope) · sign-off block. The 11 Jul milestone becomes review-and-sign.

## 10 Jul — FINISHING PASS: TRAINING EFFECT + AVATAR UPLOAD + REAL BLE
- **Per-session TRAINING EFFECT (US35 short-term ✅):** shared card on #10 Summary + #12.1 Detail per the spec — band (Low/Moderate/High/Very High, coloured) + 1–10 score + canned recovery line; Premium adds the **aerobic/anaerobic split + indicative recovery window**; Free gets the upsell link; no-HR sessions get the honest unavailable state. Pure rules in `entities/training_effect.dart` (spec formula; 7 tests). Verified: Alex's Leg day = High · 8/10, aerobic 4.0/5, ~24 h.
- **Avatar upload (StorageGateway lands):** public `avatars` bucket (owner-folder write policies, migration `20260709100000`), `StorageGateway.uploadAvatar` (upsert + cache-busted public URL), `UpdateAvatar` control, gallery picker on #13 (image_picker, 512px), photo renders on the profile circle + AvatarButton. Verified end-to-end on the simulator (storage object + profiles.avatar_url + rendering).
- **Real BLE heart rate (the promised class swap):** `HrSource` abstraction over the simulated `WearableHrSource` and the new **`BleHeartRateSource`** (flutter_blue_plus; GATT Heart Rate 0x180D / measurement 0x2A37, uint8/uint16 parser unit-tested). The #7.1 pairing sheet now runs a **real scan** — finds list above the demo devices ("NEARBY (BLUETOOTH)"); real pairings store `ble_remote_id` (migration `20260709110000`) and ActiveWorkout streams live HR, **falling back to the simulated stream** if the connect fails. Sim-safe verified (scan yields nothing without Bluetooth; demo path unchanged). ⚠️ Live-hardware pass pending — same physical-device session as the notification check.
- **221 tests.** Migrations applied local + hosted. iOS Info.plist gains photo-library + Bluetooth usage strings.

## 9 Jul (late) — DEMO SCRIPT WRITTEN
- **[demo-script.md](app/demo-script.md)** — the Aug final-demo run-of-show: pre-demo checklist (reseed, Mia-revert SQL, backend choice, AI-key check, device for notifications), five acts (~12 min: core loop as Mia → social → live premium upgrade → marketplace client side → expert portal + notifications) with per-step talking points tying features to the architecture, a wrap line, and a risk/fallback table. Demo polish is **done** — the roadmap to the demo is now rehearsal + the standing device-notification check.

## 9 Jul (late) — DEMO POLISH: BACKGROUND ATHLETES
- **seed-demo.sql §9**: three background athletes — Jordan Lee (runner), Priya Nair (yoga/trails), Leo Chen (cyclist) — with now()-relative sessions, XP/streak via the canonical formula, one shared post each, mutual friendships with Mia + Alex, likes/comments both ways, and challenge entries (20 IN 30 ×3, RUN 100K, BURN 5K ×2, LONG RIDE). The #11 feed now has five voices and the 20 IN 30 leaderboard **5 participants** (verified live as Alex: Jordan's + Leo's posts w/ likes+comments; podium Mia 6 · You 6 · Leo 4). Applied to **local AND hosted**. 

## 9 Jul (evening) — HISTORY→SOCIAL LINK (the last app-tail item)
- A shared session's #12.1 detail now shows **VIEW SHARED POST** → #11.1 Post Detail (likes + comments) — closing the capture → analyse → share → engage loop in one flow. `SocialGateway.findSharePostId` + `sessionSharePostProvider` (family, null when unshared/signed-out; 3 tests). Verified live as Alex: 10k tempo detail → post with 1 like + Mia's comment. **210 tests.** The app tail is done — next: demo polish (seed athletes, demo script).

## 9 Jul (later) — #12.2 ADVANCED WORKOUT ANALYTICS (US34 ✅, US35 🟨)
- **The Premium drill-in built to its spec** (`advanced_analytics_screen.dart`, reached via the new **Advanced ›** action on the Basic Workout Analytics card — Premium-only, no locked teaser per spec): now-anchored **ACWR tile** (acute 7d ÷ chronic 28d, four bands, thin-history state), range pills (4 wks/3 mo/1 yr/All) scoping **weekly volume** (sessions/minutes/calories toggle), **HR efficiency** (avg HR per week, honest framing), **training load** (+ factual >50% acute-spike caption), **Karvonen HR-zone** stacked bar (footer cites resting/max HR), and all-time **personal bests**.
- **All maths is a pure entity module** (`entities/advanced_analytics.dart`, 13 unit tests): sessionLoad (Karvonen-flavoured 1–10), computeAcwr, weeklyBuckets (zero-filled weeks), computeHrZones (avg-HR fallback path), computePersonalBests. Documented deviations: no mock `computeTrainingEffect` port (own load score), zones always use the avg-HR fallback (track points carry GPS not per-second HR), bests swap strength kg×reps for "longest session". Charts are lightweight bar rows — no chart package.
- **207 tests.** Verified live as Alex: ACWR 1.87 OVERREACHING (his seed clusters recent), metric toggle, zone split 17/69/14, bests incl. 22 km · 2:44/km · 3-day streak.

## 9 Jul — HISTORY SEARCH (Premium, #12 §Search)
- The 🔒 pill becomes a real Premium search field ("Search history by name or type", × clear): case-insensitive substring on custom name + resolved type name via the pure `filterSessionsByQuery` (in `workout_history.dart`, unit-tested). **Both the Basic Workout Analytics aggregates and the session groups narrow live**; zero matches render `No history matches "{q}".` per spec. Free keeps the locked pill, now routing to **#16 Upgrade** (point-of-friction upsell, per spec — replaced the old "later sprint" snackbar). **194 tests** (+4). Verified live as Alex ("leg" → 1 session + recomputed aggregates; "legzzz" → miss state) and as Mia (pill → Upgrade).

## 8 Jul (evening) — NOTIFICATIONS BUILT (US19–US21 ✅)
- **`NotificationGateway`** (new gateway boundary; `flutter_local_notifications` + `timezone` + `flutter_timezone`): explicit permission ask, one-shot zoned scheduling, cancelAll, pending count. **Timezone gotcha:** the plugin schedules off the TZDateTime's *wall-clock components*, so `tz.local` must be the device IANA zone — a UTC-instant shortcut silently lands ~8h off; `flutter_timezone` resolves the real zone at init.
- **`ScheduleReminders` control** — the RULE-BASED engine (SRS §3.9: not AI). A sync recomputes the next 7 days of one-shot notifications from prefs + plan + history: **daily plan-day nudges** at 08:00 (Premium adapts to the median session start hour; a late nudge fires if today's hour slipped, cutoff 21:00), **missed-workout** catch-up at 09:00, **inactivity** 3 days after the last session, **rest alert** (Premium-only, WBS wins over SRS wording — recon log C2b) on 3+ sessions in 3 days. Synced on athlete shell load (one permission ask) and on #13.4 toggle flips. New `rest_alert` pref key.
- **#13.4 gains an UPCOMING strip** — what the last sync actually scheduled (title + relative day/time), so the demo can *show* the schedule without waiting for a fire.
- **Verified on the simulator:** permission prompt → granted; sync scheduled the overdue inactivity alert (correct Asia/Singapore wall-clock in the strip); `pending=1` confirmed held by the OS. **iOS 26 simulator does not deliver calendar-trigger notifications** (known sim flakiness) — the display path was proven separately with `simctl push` of the identical payload (banner rendered). ⚠️ Do a physical-device (or Android emulator) pass before the demo.
- **188 tests** (+11 covering every rule incl. negatives). Demo data restored via a full `seed-demo.sql` re-run — which also restored the **pending service-request demo state** (a demo-polish item).

## 8 Jul (later) — EXPERT PORTAL COMPLETED (US42–US47, US49–US50 ✅)
- **#21.2 Create/Edit Service built** (`service_editor_screen.dart`, `PublishService` control — bce §2.4 name, SEQ `publish-service`): full listing editor (name, description, category from the catalog, fulfillment, price, one-time/monthly, response-time pills, bullets, accepting-bookings, **draft/live/archived status**). ⊕ on #21 creates; tapping a listing edits. Saving live publishes straight to the client marketplace (verified in-DB: new listing row `live` immediately).
- **#24.1 Manage Professional Info built** (`professional_info_screen.dart`, `UpdateExpertProfile`, SEQ `manage-professional-info`): title, years, about, credentials (one per line), specialties (comma-separated) — reached from a new row on #24.
- **Backend hardening** (migration `20260708100000`, local + hosted): **column-level UPDATE grant on `expert_profiles`** — authenticated may update only the five self-descriptive columns; `rating_avg`/`review_count`/`client_count`/`total_earned_cents`/`verification_status` are grant-revoked and change only via the SECURITY DEFINER RPCs (verified: owner UPDATE on `rating_avg` and `verification_status` both raise `insufficient_privilege`).
- **Theme fix found by verification:** dropdown menu items rendered near-invisible (inherited style on the tinted menu surface) — `DropdownButtonFormField`s now set explicit `AppTypography.body` + `dropdownColor: surface`.
- **177 tests** (+3: create-vs-update dispatch, profile-update payload, enum wire values). Demo data restored to seed state after verification (Nutrition Kickstart test listing deleted; Sam back to 9 yrs / strength+mobility). Remaining expert-side gaps: US41 (expert signup — website scope), US48 (content publishing — out of scope per docs), US51 rich composer, US52 admin verification (admin portal).

## 8 Jul — PREMIUM UPGRADE FLOW LIVE (US31 + US40 ✅)
- **`start_premium()` RPC** (migration `20260708090000`): caller must be role `free`; flips `role → premium` and upserts the `subscriptions` row (`active`, renews +1 month, 999¢). The role-guard trigger (`guard_profile_privileged_columns`) now admits the flip via a **transaction-local GUC** the RPC sets — direct client role writes stay blocked (verified: second upgrade call and direct `role='admin'` UPDATE both still raise). Applied to **local AND hosted**; advisors show only the by-design SECURITY DEFINER WARNs.
- **#16 Upgrade built** (`ui/premium/upgrade_screen.dart`): hero + the 6 honest unlocks + $9.99 pricing card + START PREMIUM → **simulated-payment sheet** (plan summary, mock Visa •••• 4242, CONFIRM PAYMENT) → live flip, no re-login. Wired from every Free upsell hook: Dashboard **Go Premium banner** (new), Profile GO PREMIUM pill, History unlock pill + monthly-cap banner + empty-cap state, Plan Detail regen CTA + workout-detail teaser.
- **#13.6 Subscription Management built** (`ui/premium/subscription_management_screen.dart`): gold-ringed plan card w/ status badge + renewal line, mock payment method, **synthesised billing history** (entity rule `Subscription.billingDates` — one charge/month from `startedAt`, 12 most recent), Cancel (confirm dialog; keeps access until renewal) / Resume. Premium-only ⭐ Manage Subscription row on #13 Profile.
- **BCE additions:** `Subscription` entity (+`SubscriptionStatus`), `startPremium`/`fetchSubscription`/`setSubscriptionStatus` on ProfileGateway, `StartPremium` + `ManageSubscription` controls (SEQ `start-premium` / `manage-subscription`). **174 tests** (+6: upgrade invalidation, non-free rejection, cancel/resume writes, billing-date rules).
- **Verified live on the simulator as Mia:** banner → #16 → sheet → confirm → "Welcome to Premium 🎉", banner gone, pill gone, Manage Subscription appeared, #13.6 active → cancelled ("access until" date) → resumed; DB row checked at each step. **Mia then reset to Free** (role + subscription row removed) to restore the demo state.

## 7 Jul — EXPERTS CLUSTER BUILT (US27–US29 ✅, US49–US51 🟨)
- **Marketplace live end-to-end** (commits e50b66a · 73d58cf · 3c4c1f9 · cf0f268): #6 Experts tab (Experts/Service Listings sub-tabs, search + category chips, follow-heart bookmarks on `profiles.followed_expert_ids`), #6.1 Expert Detail (stored aggregates, credentials, specialties), #6.2 Service Detail (deliverable cards from jsonb sections + the pinned footer state machine: Request · $X → pending → in progress → Leave a review → ✓ Reviewed), Dashboard **MY PURCHASES** strip, and the **minimal expert view** — a role=expert account's Experts tab becomes their request inbox (Accept/Decline, deliverable composer, Mark complete w/ confirm). The #20–24 expert portal remains deferred (deliberate realization; no spec files exist).
- **Backend:** `accept/decline/complete_service_request` + `submit_expert_review` SECURITY DEFINER RPCs (party + status-lifecycle guards; review recomputes rating_avg/review_count; complete bumps client_count) — the transition rules the RLS starter deferred — plus a **grants lockdown** (direct UPDATE on service_requests and all writes on expert_reviews revoked; RPCs are the only path). Demo seed: third account `expert@wiseworkout.test` (Sam Rivera, verified Strength Coach, 3 live services) + engagements covering every footer state. **Applied to local AND hosted** (advisors: only the by-design RPC WARNs).
- **Verified live, two accounts:** Mia requested the Form Check → Sam's inbox showed it verbatim → Accept → composed 'Deadlift Form Review' → Mark complete (client_count 41→42) → Mia saw the deliverable + review unlock; earlier a review submit proved the aggregate math in-DB (23→24 reviews, weighted 4.8).
- **Bug found by verification:** UNIQUE(service_request_id) makes PostgREST embed expert_reviews **one-to-one** (object, not list) — the cast threw and Riverpod served retained stale state; fixed in the gateway row parser.
- **UX pass from user feedback (7 Jul):** experts now get a **dedicated 5-tab shell** (Home · Services · Requests · Clients · Profile — the wireframes' #20–#24 track) instead of a role-swapped tab, with an expert dashboard and expert profile; all text fields have visible boxes (theme hairline — surface-on-surface fill was invisible) with labels outside the box; request-card actions are matched 44px button pairs; MY PURCHASES rows restructured (full title row + meta/price/badge row, no truncation).
- **Expert earnings added (7 Jul):** `expert_profiles.total_earned_cents` — a simulated-earnings aggregate (not a ledger): `complete_service_request` adds the engagement's quoted price; backfilled from completed rows; shown on the expert dashboard + profile (EARNED tile). Applied local + hosted.
- **Wireframe-fidelity fix (7 Jul, post-audit):** deliver/complete moved to match the fulfillment model — #22 Requests is now **triage-only** (accept/decline; an accepted row shows "In progress — manage under Clients" linking onward), #23 Clients splits **Active/Past** with tappable rows, and the new **#23.1 Client Detail** (`expert_client_detail_screen.dart`) hosts Send deliverable + Mark complete per engagement. Remaining known gaps vs the mock: #21.1/#21.2 Create-Edit Service and #24.1 Manage Professional Info (documented-deferred).
- **Tests 168** (146 → 158 → 163 → 168). Payment remains simulated (price snapshots only). Trims: no Following view, non-interactive sort, expert content US30/48 out of scope (services-only per docs), simplified composer.

## 6 Jul — SOCIAL CLUSTER BUILT (US22–US25 ✅) + local Supabase stack
- **All four social screens live** (#11 Social w/ Community+Challenges tabs, #11.1 Post Detail, #11.2 User Profile, #11.3 Challenge Detail), built in 3 verified phases (commits e1a1079 · 23a4089 · f8c3b4a): polymorphic feed (workout_share / level_up / challenge_result), likes + flat comments + owner caption edit, mutual friends (search, Add Friend/Unfriend, profile stats), challenges (Joined/Active/Past, join/leave/create w/ creator auto-join, **live-computed leaderboards**). Trims per the built-simpler precedent are listed in the plan (`~/.claude/plans/nested-questing-penguin.md`) and mirrored in the spec files.
- **Backend additions:** `add_friend`/`remove_friend` SECURITY DEFINER RPCs (mutual pair unwritable under `follows` RLS), `challenge_leaderboards(uuid[])` invoker fn over `public_workout_sessions`, baseline table grants (local parity), 5-challenge public catalog in seed.sql, demo friendship/likes/comments/participation in seed-demo.sql. ✅ **Hosted synced (6 Jul, after local verification):** both remaining migrations applied via MCP, challenge catalog + refreshed demo seed run, leaderboard RPC verified (identical to local), security advisors clean (the 2 new WARNs are the friend RPCs being authenticated-callable — by design).
- **Local dev stack now exists:** `supabase init` (config.toml in repo, ports 55321-9 to coexist with other projects), `supabase start` → migrations+seed auto-apply; run the app against it with `--dart-define=SUPABASE_URL=http://127.0.0.1:55321 --dart-define=SUPABASE_ANON_KEY=<local publishable key from supabase start>`. Local testing found 2 real gaps: missing baseline grants locally, and **stale fixed demo dates** — seed-demo.sql session dates are now `now()`-relative (evergreen demo; previous-month rows keep the Free-cap demo alive).
- **Bug fixed:** `relativeDay()` compared UTC components → "Yesterday" for fresh posts near midnight; now compares local dates (+ regression test). Affected History too.
- **Tests 146** (112 → 126 → 133 → 146); every phase verified on-simulator via **idb** (installed + patched for Python 3.14 — taps/typing/screenshots are now scriptable) incl. RLS probes (impersonated like rejected; create-as-other rejected).

## 6 Jul — simplify pass applied (whole [simplify.md](app/simplify.md) list)
- **All H1–H8 / M1–M5 / L1–L5 items applied**, one at a time, each verified (analyze + 112 tests + simulator relaunch). Net ≈ **−250 lines** in `lib/` with zero visual/behaviour change.
- **Shared widget library born:** `boundaries/ui/common/` = `StatTile` · `AppCard` · `StatusBadge` · `PremiumCta` (+ existing `AvatarButton`); plus `core/theme/app_buttons.dart` (outlined accent/danger styles), `core/strings.dart` (`isBlank`), `fmtCompactNum` in `core/format.dart`.
- **Logic DRY:** `PlanGateway._deactivatePriorPlan`, `FitnessGoal.hasTargetFor`, `Profile.isFree`, `FitnessProfile.ageFrom` (screen's duplicate `_age()` deleted); `intl` dependency removed; dead `providesHeartRate` getter removed.
- **Analysis corrected where wrong:** H5/H6/H7 partly, L3/L4 fully were false positives — logged in simplify.md Corrections so the presentation cites accurate numbers.

## 5 Jul — repo restructured: app/ vs docs/
- **Two-folder split for the presentation:** everything needed to run the product now lives in **`app/`** (the whole Flutter project + `app/supabase/` backend); everything else stays in **`docs/`** (planning/design docs; `scripts/` moved to `docs/scripts/`). Moves done with `git mv` — history preserved. **Run all Flutter commands from `app/`.**
- No code changes — `flutter analyze` clean, **112 tests** green, app re-verified on the iOS simulator post-move. Cross-links in README/CLAUDE.md/docs fixed (`../supabase` → `../app/supabase`, etc.).

## 13 Jun — PTD assembly + sequence-diagram pass
- **PTD assembled** into the v1 Word template (A4, auto-numbered headings): `../FYP_docs/Submissions/PTD/FYP-26-S2-37-PTD-v1-FILLED.docx` (outside this repo). Content sources also kept in-repo-adjacent as `PTD-content.md` (markdown) + `PTD-content-tabs.txt` (tab tables). ~23k words / 93 tables — at PRD/SRS depth: full competitor write-ups, comparison matrix, SWOT, all **64 user stories** + **64 full use-case descriptions**, FR1-12, 6 NFR categories (31 NFR-IDs), 15-risk register, business model (Free-vs-Premium matrix + cost + channels), charter, comms plan, glossary, appendix. Figures renumbered **1-20**; §15 repointed to the real use-case diagrams (SRS §4 / PRD §7.2).
- **All 59 sequence diagrams** rewritten so arrow labels are the **real functions** (Control methods + gateway/RPC/Edge functions; built = `lib/` names, unbuilt = bce-design §2.4 planned names). Re-rendered. Convention recorded in the seq-diagrams skill.
- **Test count confirmed 109** via `flutter test` (13 Jun) — PTD p.58 + STATUS aligned. (An earlier pass had pre-emptively "corrected" it to 107; the suite has since grown back to 109, so 109 is canonical.)
- **Sample-template wording scrubbed** from the PTD (top-to-bottom check): §4 "public property datasets" → "external or third-party datasets"; Appendix B sample-citation bullet removed. The scrub is baked into `scripts/expand_ptd.py` so regen keeps it clean. ("AI / ML Usage" in §2.1 is legit competitor-comparison content, not sample text.) PRD/SRS/TDM Word docs were also scanned — clean, no sample leftovers.
- **PTD + PUM reframed to whole-app** (decided 13 Jun): all "prototype" framing removed (51 instances in the PTD, 4 in the PUM draft) so the deliverables describe the full product. Honest current-state kept only as brief notes — simulated payment, mock-BLE wearable (Appendix C "Implementation Status Notes" + SWOT), 109 tests, "(simulated)" on wearable HR. Reframes are in `scripts/expand_ptd.py` (PTD) and `pum-net-new-sections.md` (PUM).
- **Deferred:** ~~reconciliation **B5**~~ ✅ done — the Product Comparison Matrix was rebuilt to back the USP (added verified-expert-marketplace + à-la-carte expert-services rows; AI row no longer claims adaptive coaching). ~~Confirm test count~~ ✅ confirmed 109.

## 13 Jun — PUM assembled (real app screenshots)
- **PUM built** via `scripts/build_pum.py` → `../FYP_docs/Submissions/PUM/FYP-26-S2-37-PUM-v1-FILLED.docx` (filled from the v1 PUM Word template; cover, PUM header/footer and the TOC content-control preserved). Single-source content in `PUM-content.md` alongside it. Structure mirrors the sample PUM: Document Version Control → Introduction → Installation → Key Features → Initial GUIs (screen walkthrough). Re-run the script to regenerate.
- **Built-only scope:** the walkthrough covers the **19 screens actually built**. Unbuilt screens were dropped (Experts cluster, Social cluster, Premium upgrade + subscription, Advanced Analytics, Expert/Admin portals, web Register), and §1 Introduction + §3 Key Features were trimmed to match. §4 reordered to the real journey: Getting Started → Home → Training & Plans → Recording a Workout → History → Profile/Goals/Settings.
- **Each screen = a description naming the on-screen elements + a "How to use:" step list**, then a **real app screenshot embedded inline** (captured from the iOS simulator with `simctl`, demo account Mia). 19/19 embedded, no placeholders. PNGs in `../FYP_docs/Submissions/PUM/screenshots/`.
- **Demo seed for the shots:** reran `seed-demo.sql` and added an active goal + 12-week plan + a week of `planned_workouts` for `free@` so Home/Train/My Plans/Plan Detail show real content. (That goal/plan seed was ad-hoc SQL, not added to `seed-demo.sql`.)
- **⚠️ Spec-vs-build drift found** while screenshotting — several built screens are simpler than their [reference/screens/](app/reference/screens/) specs: **Dashboard** is the minimal "Get moving" slice (no digest card/weekly stats/goal), **Fitness Goals** has no target/timeline controls, **Account Settings** has no phone field, **Login** has no "Remember me", **Train** card is compact (no VIEW WORKOUT/START PLAN buttons). PUM descriptions were corrected to the actual build; the screen specs still describe the fuller design — treat those specs as design intent, not the current build.
- **Manual step left:** open the FILLED docx and update the TOC field (right-click → Update Field → Update entire table).

## Calendar
- **~13 Jun** — PTD + PUM due (basic-prototype milestone, PRD §8.2).
- **20 Jun** — End-of-Term-1 review.
- 11 Jul module testing (report ready — [testing/module-test-report.md](app/testing/module-test-report.md)) · 1 Aug integration · 13–22 Aug final demo.

---

## Done (this work block)
1. **Reconciled all docs to PRD v2.0 + SRS v2.0**, then to **TDM v3.0** (5 Jun; **superseded by TDM v5 on 12 Jun** — v5 canonical except §6). Engineering decisions lead; submitted docs follow.
2. **Database question resolved** — the TDM §8 ERD is the schema of record (`ExpertReview` kept; expert layer = `ExpertService → ServiceRequest → Deliverable`; payment simulated). database-v1.md flagged to align to it.
3. **Cross-doc change log created** — [deliverables/doc-reconciliation-log.md](deliverables/doc-reconciliation-log.md): every edit needed in PRD/SRS/TDM, plus TDM-internal fixes.
4. **Price decided** — premium = **$9.99/mo** (was $9.90 on the website page).
5. **PTD net-new sections drafted** — [deliverables/ptd-net-new-sections.md](deliverables/ptd-net-new-sections.md) (SWOT, USP, charter, comms plan, legal/regulatory, level-1 DFD, glossary).
6. **PUM net-new sections drafted** — [deliverables/pum-net-new-sections.md](deliverables/pum-net-new-sections.md) (doc control, intro, install, key features, 10-screen walkthrough).
7. **bce-design.md aligned** to the narrow AI scope — `GenerateFitnessPlan` → `BuildPlanSkeleton` (rule) + `SuggestPlan` (AI); added `SummariseProgress`; `OpenAIPlanGateway` → `AiGateway`. Control list, traceability matrix, robustness §4.2 and sequence §5.3 all updated.
8. **CLAUDE.md / README aligned** — TDM marked canonical (v3.0 then, **v5 since 12 Jun**); new docs indexed; settled figures noted.

## Open — team's Word assembly (no code, due ~13 Jun)
- Assemble PTD: copy/reformat PRD+SRS+TDM into the 18-section structure; drop in the net-new drafts; render diagrams to PNG.
- Assemble PUM: layout the net-new drafts + mock/TDM §7 screenshots.
- **Apply reconciliation-log §B during PTD assembly** (Supabase stack, $9.99, simulated payment, AI scope) — PRD v3 didn't fold them in; the PRD itself is fixed *after* submission (only PTD+PUM are submitted — decided 12 Jun).
- **PTD sequence diagrams: source from bce-design.md §5, NOT TDM §6** — TDM v5's §6 diagrams are wrong (team-confirmed 12 Jun; log A3). TDM v5 is otherwise canonical.

> 🐛 **Bug log:** [testing/bug-log.md](app/testing/bug-log.md) — every defect with root cause + fix commit (16 app bugs fixed, 5 doc defects, 4 open watch items).

> 📋 **Story-level progress:** [requirements/user-stories.md](requirements/user-stories.md) — all 64 SRS user stories with build status (✅/🟨/⬜), updated per cluster.

## Next — engineering (after the 13th; not blocked)
1. ✅ **Aligned [reference/database-v1.md](app/reference/database-v1.md) to the TDM §8 ERD** (rosters match at 26 entities) and generated **Postgres DDL + RLS starter + seed** → [`/supabase/`](../app/supabase/) (`migrations/` + `seed.sql`; see [supabase/README.md](../app/supabase/README.md)). ✅ **Applied to the hosted Supabase project** (`zbeyytgilrqruttvecdc`) via MCP on 10 Jun — 26 tables · 28 enums · 49 policies · 2 privacy views · catalogs seeded; security advisor triaged (trigger-function EXECUTE revoked; the 2 SECURITY DEFINER privacy views are intentional). *Still to do:* the SECURITY DEFINER RPCs (`endWorkoutSession`, `startPremium`, request transitions) when their controls land; note the MCP-applied migration history uses its own version ids — reconcile if/when adopting the Supabase CLI `db push`.
2. ✅ **Scaffolded the Flutter project** (10 Jun) — `flutter create` (pkg `wise_workout`, android/ios/web), BCE folders (`lib/entities`, `lib/controls`, `lib/boundaries/{ui,gateways}`), deps wired (flutter_riverpod, go_router, freezed + json_serializable, supabase_flutter, geolocator/pedometer, flutter_local_notifications), design tokens in `lib/core/theme/` (palette + iOS type scale), Supabase client initialized in `main.dart` (publishable key via `lib/core/config/env.dart`), go_router skeleton (Splash → Home), `AuthGateway` boundary. `flutter analyze` clean · widget test passes · `flutter build web` succeeds. *Toolchain note:* Android SDK + Xcode/CocoaPods not yet installed — web + connected devices work now.
3. **Vertical slice** — log in → record a phone-GPS workout → history → AI summary → share. (build-plan §7.) Plan: [~/.claude/plans/nested-questing-raccoon.md]; 5 phases (auth · capture · history · AI summary · share); decisions: slice-only, AI stubbed behind AiGateway (stub since replaced by live OpenAI, 12 Jun), test accounts via MCP.
   - ✅ **Phase 1 (auth slice + shell) done & verified on Android + iOS** (10 Jun): first freezed entity (`Profile`) + codegen pipeline; `build.yaml` snake_case; `seq_log`; `Authenticate` control + `ProfileGateway`; Login/Splash/HomeShell + go_router auth redirect. Two test accounts seeded in Supabase Auth (`free@`/`premium@wiseworkout.test`, pw `Password123!`). REST-verified auth + RLS (own-row only); login screen renders on emulator + iOS simulator. *Toolchain note:* manually-seeded `auth.users` needed empty-string token columns (not NULL) to avoid GoTrue 500s.
   - **Toolchains set up (10 Jun):** openjdk@17 + Android cmdline-tools/SDK/emulator (`pixel_api35` AVD), Xcode 26.5 + CocoaPods + iOS 26.5 simulator runtime. Android build needed core-library desugaring for flutter_local_notifications (in `android/app/build.gradle.kts`).
   - ✅ **Phase 2 (capture)** — `WorkoutDataSource`/`PhoneSensorSource` + permissions, `end_workout_session` RPC (XP/streak/level-up), Train/ActiveWorkout/Summary (spec-matched to #7/#9).
   - ✅ **Phase 3 (history)** — History (#12, analytics + grouped cards) + History Detail (#12.1, edit/delete); matches TDM activity diagram.
   - ✅ **Phase 4 (AI summary)** — `summarise-progress` Edge Function (stub, swappable) + `AiGateway` + `SummariseProgress` + History ✨ sheet (AI-assisted label).
   - ✅ **Phase 5 (share)** — `SocialShareGateway` (FB/IG/Twitter/TikTok) + `CreateWorkoutSharePost`/`ShareWorkoutToSocial` + Summary share section (creates `workout_share` Post).
   - ✅ **Vertical slice COMPLETE** — log in → record phone-GPS workout → history → AI summary → share, verified on Android emulator + iOS simulator. Test accounts: `free@`/`premium@wiseworkout.test` (pw `Password123!`).
4. **Breadth build-out** (11–12 Jun, with Claude Code; order: profile → plans → social → experts → premium → dashboard → portals):
   - ✅ **5-tab bottom nav** per spec (Home · Experts · Train · Social · History); Experts/Social are styled later-sprint placeholders (11 Jun).
   - ✅ **Onboarding + Plans COMPLETE** (12 Jun) — first-login wizard (#3: welcome → body metrics + name fallback → training context (+ custom workout types) → goal → AI plan), gated on `profiles.onboarding_completed_at`. **Both tiers get AI plans** (decision per WBS/SRS; recon C4 cancelled): `suggest-plan` Edge Function, Free basic / Premium personalised, **full timeline generation** (foundation→build→peak→recovery across `timeline_weeks`), preferences are a strict contract; `BuildPlanSkeleton` = rule fallback. Train shows the selected-plan card (week schedule + next-session highlight + **Start Planned Workout** for today, alongside the sticky freeform capture); **My Plans** lists active/saved plans and lets users activate an inactive plan; **Plan Detail #8** supports dynamic week selection + workout modal and is **view-only** (start moved to the Train card), regenerate w/ Free **1/month** (resets each calendar month; was a cumulative cap — fixed, see bug-log).
   - ✅ **AI LIVE** (12 Jun) — both Edge Functions call **OpenAI gpt-4o-mini** (strict JSON schema + server-side validation for plans; Gemini → deterministic stub degradation). Key in Supabase Edge Function secrets; never ships in the app.
   - ✅ **Connected devices #7.1** (12 Jun) — mock-BLE pairing (spec-sanctioned), phone-sensors pinned system device, simulated wearable HR streamed live into capture, avg/max HR persisted, sessions linked to their source device, last-synced stamps.
   - ✅ **Tiering + data correctness** (12 Jun) — Free history capped to the calendar month at the query level (Profile lifetime stats exempt per #13); MET calorie estimates computed at session end and saved; custom workout types + health tags creator-private via RLS (token-probe verified). **109 tests** green.
   - ✅ **Profile cluster (#13 + 13.1–13.5 + #4) COMPLETE** (12 Jun) — Profile hub (identity, level/XP bar, lifetime stats, Go Premium pill, log out), Fitness Profile (body metrics + activity/experience + preferred workouts + diet/allergy/injury pickers w/ custom tags, batched save), Fitness Goals (goal cards + target/commitment steppers + timeline, active-goal upsert), Account Settings (units instant-commit, change-password reset email), Notifications (10 toggles → `profiles.notification_prefs` jsonb), Submit Feedback (≥10-char guard → `feedback` row, in-screen success), Forgot Password (anti-enumeration sent card). Avatar entry on Home/Train headers; every flow verified on-device against live Supabase. (**71 tests** at the time; suite now 107.)

> User flagged for the legal section: confirm whether a privacy policy + minimum-age requirement exist yet or are "preliminary."

---

## Decisions locked (don't re-litigate)
Flutter + Riverpod + go_router + freezed · **Supabase** (Postgres/Auth/Storage/Realtime) + Edge Functions · **OpenAI** primary, Gemini fallback · AI = summaries + suggestions only · three-layer model (Free / Premium / à-la-carte Expert services) · simulated payment · BCE architecture · named social platforms (FB/IG/Twitter/TikTok) · premium $9.99/mo. Rationale lives in [architecture/build-plan.md](app/architecture/build-plan.md) and CLAUDE.md.
