# json_serializable: map Dart camelCase <-> Supabase snake_case columns globally,
# so freezed entities don't need a @JsonKey on every field.
targets:
  $default:
    builders:
      json_serializable:
        options:
          field_rename: snake
          explicit_to_json: true
